// Adresse zerteilen
$(document).ready(function() {
		var b = 'markus',
			n = 'schmieder',
			domain = 'gmx',
			topdom = 'net';
		
		$('.to').html(b + "-" + n + "@" + domain + "." + topdom);
			});